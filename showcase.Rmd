---
title: "Showcase"
description: |
  A showcase of distill websites and blogs!
output: 
  distill::distill_article:
    toc: true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
    echo = FALSE, 
    message = FALSE, 
    warning = FALSE
)

# Load libraries and functions
library(tidyverse)
library(webshot2)
source(file.path("R", "functions.R"))

# Load data
sites <- clean_sites(read_csv("sites.csv"))
```

```{r update-screenshots, echo=FALSE}
update_screenshots(sites)
```

```{r insert-showcase, echo=FALSE}
child_docs <- update_child_docs(sites)
child_docs <- file.path("rmd_chunks", list.files("rmd_chunks"))
```

```{r, child = child_docs}
```
